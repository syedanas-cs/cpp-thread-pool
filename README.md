# C++ Thread Pool

A **simple and efficient C++ Thread Pool** implementation with support for **std::future**.  
This project demonstrates how to manage multiple threads, enqueue tasks, and retrieve results asynchronously — perfect for backend development.

---

## Features

- Fixed-size thread pool
- Task enqueueing with **return values** (`std::future`)
- Thread-safe queue
- Easy to integrate into C++ projects

---

## Build Instructions

```bash
mkdir build
cd build
cmake ..
cmake --build .



